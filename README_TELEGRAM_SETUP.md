# Настройка формы обратной связи с Telegram

## Требования

1. Telegram бот с токеном (уже предоставлен)
2. Chat ID получателя сообщений

## Получение Chat ID

Есть несколько способов получить ваш Chat ID:

### Способ 1: Через бота @userinfobot
1. Откройте Telegram
2. Найдите бота @userinfobot
3. Отправьте ему любое сообщение
4. Бот вернет ваш Chat ID

### Способ 2: Через getUpdates API
1. Откройте в браузере: `https://api.telegram.org/bot{ВАШ_ТОКЕН}/getUpdates`
2. Найдите в ответе `"chat":{"id":123456789}`
3. Это и есть ваш Chat ID

### Способ 3: Автоматически (в коде)
Код автоматически попытается получить Chat ID через `getUpdates`, если `TELEGRAM_CHAT_ID` не задан.

## Настройка переменных окружения

### Для локальной разработки

Создайте файл `.env` в корне проекта:

```env
TELEGRAM_BOT_TOKEN=8548638828:AAFm1l0cnI2CLy4GDjedC2V37OqiAE_ItzU
TELEGRAM_CHAT_ID=ваш_chat_id_здесь
VITE_API_URL=/api/telegram-send
```

### Для Vercel

1. Перейдите в настройки проекта на Vercel
2. Откройте раздел "Environment Variables"
3. Добавьте:
   - `TELEGRAM_BOT_TOKEN` = `8548638828:AAFm1l0cnI2CLy4GDjedC2V37OqiAE_ItzU`
   - `TELEGRAM_CHAT_ID` = ваш Chat ID

### Для Netlify

1. Перейдите в настройки сайта на Netlify
2. Откройте "Environment variables"
3. Добавьте те же переменные

## Структура API

API endpoint находится в `api/telegram-send.ts` и работает как serverless функция.

### Формат запроса

```json
{
  "name": "Имя пользователя",
  "platform": "instagram" | "telegram",
  "contact": "@username или номер",
  "message": "Текст сообщения"
}
```

### Формат ответа

Успех:
```json
{
  "success": true,
  "message": "Message sent successfully"
}
```

Ошибка:
```json
{
  "error": "Описание ошибки"
}
```

## Тестирование

1. Запустите проект: `npm run dev`
2. Перейдите на страницу с формой
3. Заполните форму и отправьте
4. Проверьте Telegram - должно прийти сообщение

## Формат сообщения в Telegram

Сообщения приходят в следующем формате:

```
📧 Новая заявка от: {имя}
📱 Платформа: {Instagram/Telegram}
👤 Контакт: {контакт}
💬 Сообщение:
{сообщение}
```

## Безопасность

⚠️ **Важно**: Токен бота должен храниться только в переменных окружения на сервере. Никогда не коммитьте `.env` файл в репозиторий!

Файл `.env` уже добавлен в `.gitignore`.



